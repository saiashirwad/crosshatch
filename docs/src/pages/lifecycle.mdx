---
title: Lifecycle
description:
  Follow a Crosshatch session from the initial x402 requirement through wallet setup, payment signing, and resource
  delivery.
---

# Lifecycle

Crosshatch fits between apps that request paid resources and merchants that return x402 payment requirements. Apps do
not hold user private keys, register server-side API keys, or maintain separate staging payment credentials. The same
client integration can work end-to-end in development and production because Crosshatch owns wallet setup, funding,
allowance approval, and payment payload generation.

This makes Crosshatch especially useful for generated or rapidly assembled app UIs: when a paid resource returns `402`,
the app can ask Crosshatch for a payment payload and retry the request without first provisioning merchant-specific
payment infrastructure.

::::steps

### Initial Request

The user triggers an action in the app. The app requests a resource from a merchant, which responds with a 402.

### Payment Requirement

The merchant's `402` includes a machine-readable x402 requirement: the price, network, asset, recipient, and resource
context needed to pay for the requested resource.

### Session Check

The app asks Crosshatch whether an allowance has already been granted.

### Wallet Onboarding (if needed)

If the app lacks an allowance, a modal (or popup depending on browser) appears and guides the user through the necessary
steps: allowance approval and/or wallet funding. Once complete, the modal closes.

### Payment Signing

With a funded wallet and the appropriate approval, the app forwards the x402 requirement to Crosshatch. Crosshatch
returns a signed payment payload that the app can send back to the merchant.

:::note User private keys never leave the Crosshatch wallet context. Apps receive signed x402 payment payloads, not raw
signing material. :::

### Resource Delivery

The app retries the original request with the signed payment payload attached. The merchant verifies the payload and
returns the paid resource. The app fulfills the user's original action.

### Subsequent Payments

All future x402 payments within the user's allowance can be handled automatically - no further prompts necessary.

::::

## Diagram

The full lifecycle of a Crosshatch session is as follows - from the initial user action through wallet setup, payment
signing, and resource delivery.

```mermaid
sequenceDiagram
    autonumber
    actor User
    participant App
    participant Enclave as Crosshatch Enclave (embedded)
    participant Modal as Crosshatch Modal (embedded)
    participant Service as Crosshatch Service
    participant Merchant as Merchant Service

    Note over User: Human using the Crosshatch-integrated app
    Note over App: Integrator app (frontend logic that calls merchants and talks to Crosshatch embeds)
    Note over Enclave: A secure embedded context that lives within the app and holds user keys and vends x402 payment payloads
    Note over Modal: An overlay iframe for user interactions such as onboarding and funding
    Note over Service: Backend service that persists encrypted secrets and enforces app-specific allowances
    Note over Merchant: Third-party service that exposes resources through x402

    User->>App: Use application
    App->>Merchant: Request resource
    Merchant-->>App: Return 402 (Payment Required)
    App->>Enclave: Ask for session information
    Enclave->>Service: Ask for account information
    Service->>Enclave: Return account information
    Enclave-->>App: Return session information
    opt Crosshatch wallet needs linking, creation, and/or funding
        App->>Modal: Open modal
        Modal->>User: Prompt user to proceed with relevant setup
        User->>Modal: Combination of (A) approve app allowance, (B) create wallet, (C) fund wallet
        Modal->>Service: Save changes
        Modal-->>App: Tell user setup is complete
    end

    Note over User, Enclave: Funded flow proceeds

    App->>Enclave: Forward the x402 requirement
    Enclave->>Service: Verify 402 amount <= app-specific allowance remaining
    Service-->>Enclave: Return an encrypted secret
    Enclave->>Enclave: Decrypt secret for signing purposes
    Enclave->>Enclave: Create & sign x402 payment payload
    Enclave-->>App: Hand over signed payment payload
    App->>Merchant: Re-request resource with payment payload attached
    Merchant-->>App: Return paid resource
    App-->>User: Fulfill initial user action
    Note over App, Service: Automatically pay subsequent x402s
```
